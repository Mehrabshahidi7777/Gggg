# کارهایی که خودت باید انجام بدهی

ترتیب مهم است. از بالا به پایین برو.

---

## خلاصه‌ی چیزی که اضافه شده

| قابلیت | کاربر چه می‌بیند |
|--------|------------------|
| امتیاز ۵ ستاره | روی هر کارت و صفحه‌ی آگهی. چپ «افتضاح»، راست «بسیار عالی». بدون رفرش ثبت می‌شود. |
| نظر متنی | زیر ستاره‌ها. ثبت می‌شود ولی تا تأیید تو منتشر نمی‌شود. |
| ویرایش آگهی | ارائه‌دهنده ویرایش می‌کند، تو در پنل «قبل ← بعد» را می‌بینی و تأیید می‌کنی. |
| نمایش شماره | شماره پشت دکمه است (ضد ربات) و هر نمایش «سرنخ» شمرده می‌شود. |
| تماس‌های این ماه | کارت تازه در پنل ارائه‌دهنده. |
| یادآوری تمدید | پیامک ۷ روز مانده، ۱ روز مانده، و روز انقضا. |
| ۳۱ استان | قبلاً ۲۰ استان بود، ۱۱ استان کلاً غایب بودند. |
| آدرس فارسی | آدرس خوانا به‌جای اسلاگ بی‌معنی |
| نقشه‌ی سایت و ستاره در گوگل | خودکار |

محصول دیگر سبد خرید ندارد و مثل خدمت «تماس مستقیم» است. خرید آنلاین حذف نشده، خاموش شده.

یک باگ امنیتی هم رفع شد: هر فرد ناشناسی می‌توانست با حدس‌زدن شماره‌ی سفارش، سفارشِ در انتظار پرداختِ هر کاربری را لغو کند.

در پنل ادمین دو منوی تازه اضافه شده: «ویرایش‌های آگهی» و «نظرهای کاربران».

### یک عیب قدیمی که موقع کار روی سرچ کنسول پیدا شد

فایل `zzz/bootstrap/providers.php` از روز اول این کلاس را صدا می‌زد:

```
App\Providers\AppServiceProvider
```

ولی این فایل اصلاً وجود نداشت — پوشه‌ی `zzz/app/Providers/` خالی بود.

سایت با این حال بالا می‌آمد، و دلیلش یک اتفاق تصادفی بود: یکی از
بسته‌های `vendor` (ابزار `laravel/pint`) کلاسی با همین اسم دارد، و
PHP سراغ آن می‌رفت. یعنی روی هر درخواستِ سایت، سرویس‌پروایدرِ یک
**ابزار فرمت‌کردن کد** اجرا می‌شد.

**خطرش این بود:** اگر روزی وابستگی‌های توسعه از `vendor` حذف شوند،
آن کلاس هم می‌رفت و کل سایت با خطای ۵۰۰ می‌افتاد — بدون اینکه هیچ
تغییری در کد داده باشی.

حالا این فایل ساخته شده و همین‌جا هم اجبارِ `https` روی آدرس‌هایی که
سایت تولید می‌کند تنظیم شده (که مستقیماً به درستیِ `sitemap.xml`
ربط دارد؛ در گام ۷ توضیح داده شده).

---

## ⚠️ هشدار: کلید رمزنگاری را عوض نکن

در فایل زیر یک مقدار به اسم `APP_KEY` هست:

```
zzz/.env
```

هیچ‌وقت تغییرش نده و این دستور را اجرا نکن:

```
php artisan key:generate
```

**چرا:** شماره شبای فروشنده‌ها با همین کلید رمز می‌شود. اگر عوض شود، همه برای همیشه از دست می‌روند.

---

# گام ۱ — پشتیبان بگیر

در phpMyAdmin، دیتابیس `sazmat_sa` را انتخاب کن، بعد:

**تب Export ← دکمه Go**

فایل خروجی را نگه دار. اگر چیزی خراب شد، تنها راه برگشت همین است.

---

# گام ۲ — هشت فایل SQL را ایمپورت کن

در phpMyAdmin، دیتابیس `sazmat_sa`، بعد:

**تب Import ← انتخاب فایل ← دکمه Go**

این کار را هشت بار، یکی‌یکی، به همین ترتیب:

| # | نام فایل | چه می‌کند |
|---|---|---|
| ۱ | `01_ad_ratings.sql` | جدول امتیاز ستاره‌ای |
| ۲ | `02_provinces_cities.sql` | ۱۱ استان و ۱۴۱ شهر جدید |
| ۳ | `03_reminder_columns.sql` | ستون‌های یادآوری تمدید |
| ۴ | `04_contact_reveals.sql` | جدول ثبت نمایش شماره |
| ۵ | `05_ad_edits.sql` | جدول درخواست‌های ویرایش |
| ۶ | `06_comment_columns.sql` | ستون‌های نظر متنی |
| ۷ | `07_delete_test_ads.sql` | حذف دو آگهی تستی |
| ۸ | `08_delete_test_orders.sql` | حذف ۱۳ سفارش تستی |

همه در پوشه‌ی `deploy` هستند. اجرای دوباره‌ی هر کدام بی‌خطر است — اگر شک کردی، دوباره اجرا کن.

### فایل ۷ و ۸ اول نشانت می‌دهند چه چیزی پاک می‌شود

نگاهش کن. اگر چیزی جز داده‌ی تستی دیدی، ادامه نده و به من بگو.

فایل ۷ باید فقط دو ردیف نشان بدهد: «داب» و «قتقت».

فایل ۸ باید ۱۳ سفارش با مبلغ ۱۰۰ و ۱۰۰۰ تومان و شماره‌های الکی مثل «G5tg» و «۹۹۹» نشان بدهد.

### هنگام فایل ۸ یک نکته

گزینه‌ای در phpMyAdmin هست به اسم **Enable foreign key checks**. باید **روشن** باشد — که حالت پیش‌فرض است.

دلیلش: یک سفارش در دو جدول پخش شده، و این گزینه باعث می‌شود با حذف سفارش، آیتم‌هایش هم خودکار پاک شوند.

خود فایل آخرش بررسی می‌کند. خروجی آخرین دستور باید در هر سه ستون **صفر** باشد.

### بعد از فایل ۲ این را چک کن

در تب SQL این دو خط را اجرا کن:

```sql
SELECT COUNT(*) FROM provinces;
SELECT COUNT(*) FROM cities;
```

جواب اولی باید ۳۱ باشد، دومی ۲۱۵. اگر عدد دیگری دیدی، ایمپورت کامل نشده.

---

# گام ۳ — فایل‌ها را آپلود کن

از زیپ، محتوای پوشه‌ی `zzz` را بریز اینجا:

```
/home/sazmat/domains/sazmat.com/zzz/
```

و محتوای پوشه‌ی `public_html` را بریز اینجا:

```
/home/sazmat/domains/sazmat.com/public_html/
```

پوشه‌ی `deploy` را آپلود نکن. آن فقط فایل‌های SQL گام ۲ بود.

### ⚠️ یک فایل که احتمالاً گم می‌شود

این فایل با نقطه شروع می‌شود و بیشتر برنامه‌های FTP مخفی‌اش می‌کنند:

```
public_html/storage/.htaccess
```

در File Manager گزینه‌ی **Show hidden files** را روشن کن و مطمئن شو منتقل شده. کارش جلوگیری از اجرای اسکریپت در پوشه‌ی آپلود کاربران است.

---

# گام ۴ — کش را پاک کن

**محتویات** این دو پوشه را پاک کن. خود پوشه‌ها بمانند:

```
zzz/storage/framework/views/
zzz/bootstrap/cache/
```

اگر این فایل وجود داشت، حتماً حذفش کن:

```
zzz/bootstrap/cache/config.php
```

تا وقتی این فایل هست، تنظیمات جدید خوانده نمی‌شوند و هیچ تغییری را نمی‌بینی.

> اگر پاک‌کردن دستی برایت سخت است، در انتهای همین فایل یک راه جایگزین با کرون هست.

---

# گام ۵ — الگوی پیامکی در پنل آموت

این گام **اختیاری** است.

**اگر خط خدماتی داری:** کاری لازم نیست، پیامک به‌صورت متن ساده می‌رود.

**اگر نداری یا مطمئن نیستی:** باید یک الگو ثبت کنی.

۱. وارد پنل آموت شو:

```
portal.amootsms.com
```

۲. برو به بخش **الگوها** یا **خدمات پترن** و **ثبت الگوی جدید** را بزن.

۳. این متن را ثبت کن:

```
سازمت
%1% عزیز، اشتراک %2% شما تا %3% روز دیگر به پایان می‌رسد.
برای جلوگیری از تعلیق آگهی‌ها آن را تمدید کنید.
sazmat.com
```

۴. منتظر بمان تا تأیید شود. معمولاً چند ساعت تا یک روز کاری.

۵. بعد از تأیید، شناسه‌ی عددی الگو را کپی کن.

۶. فایل `zzz/.env` را باز کن و این خط را اضافه کن (به‌جای عدد، شناسه‌ی خودت):

```
AMOOT_PATTERN_RENEWAL_ID=12345
```

۷. دوباره محتویات `zzz/bootstrap/cache/` را پاک کن.

**معنی متغیرها:** `%1%` نام کاربر، `%2%` نوع اشتراک، `%3%` تعداد روز باقی‌مانده.

---

# گام ۶ — کرون‌جاب

**کرون جدید لازم نیست.** همان یک کرونی که داری کافی است، فقط دستورش بهتر می‌شود.

کرون باید **هر دقیقه** اجرا شود (`* * * * *`). این عجیب به نظر می‌رسد ولی درست است: لاراول هر دقیقه بیدار می‌شود، نگاه می‌کند ببیند الان موقع چه کاری است، و اگر کاری نبود بلافاصله برمی‌گردد. کارهای واقعی این‌ها هستند:

| کار | کِی |
|---|---|
| تعلیق اشتراک‌های منقضی‌شده | هر ساعت، سر ساعت |
| پاک‌سازی دائمی آگهی‌های ۶ ماه تعلیق‌مانده | هر ساعت، سر ساعت |
| پاک‌سازی شبانه (تصویر بی‌صاحب، پرداخت نیمه‌کاره، OTP منقضی) | هر شب ۰۳:۰۰ |
| پیامک یادآوری تمدید | هر روز ۱۰:۰۰ |

## دستور نهایی

این است، یک خط:

```
cd /home/sazmat/domains/sazmat.com/zzz && /opt/alt/php83/usr/bin/php artisan schedule:run --whisper 2>&1 | grep -vE "already loaded|Unable to load dynamic library|ionCube" >> /home/sazmat/domains/sazmat.com/zzz/storage/logs/cron.log
```

و زمان‌بندی:

```
* * * * *
```

سه نکته دارد که هر کدام دلیل دارند:

**۱. مسیر کاملِ PHP، نه فقط کلمه‌ی `php`**

پروژه حداقل PHP 8.2 می‌خواهد. هاست تو الان روی کرون PHP 8.3 می‌دهد و مشکلی نیست، ولی اگر روزی `PATH` هاست عوض شود، `php` خالی ممکن است بی‌خبر بیفتد روی یک نسخه‌ی قدیمی و از آن لحظه همه‌چیز بی‌صدا شکست بخورد. با مسیر کامل این ریسک بسته می‌شود.

اگر روزی مسیر عوض شد، در پنل هاست بخش تنظیمات PHP مسیر درست را نشان می‌دهد. معمولاً یکی از این شکل‌هاست:

```
/opt/alt/php83/usr/bin/php
/usr/local/bin/ea-php82
/usr/local/bin/php82
```

**۲. `--whisper`**

بدون این، لاراول هر دقیقه می‌نویسد «کاری برای اجرا نیست» — یعنی روزی ۱۴۴۰ خط بی‌فایده. با این، فقط وقتی واقعاً کاری اجرا شود چیزی می‌نویسد.

**۳. آن `grep -v`**

تنظیمات PHP خودِ هاست ایراد دارد: چند افزونه را دو بار بارگذاری می‌کند و یک افزونه‌ی خراب هم دارد. نتیجه‌اش این است که هر اجرای PHP حدود ۲۱ خط هشدار استفراغ می‌کند:

```
PHP Warning:  Module "mysqli" is already loaded in Unknown on line 0
Cannot load the ionCube PHP Loader - it was already loaded
```

**این‌ها به سایت تو ربطی ندارند و چیزی را خراب نمی‌کنند**، فقط لاگ را پر می‌کنند — اندازه‌گیری شد: حدود ۱ گیگابایت در سال. آن `grep -v` دورشان می‌ریزد.

## چطور مطمئن شوی کار می‌کند

این فایل تنها راه فهمیدن است:

```
/home/sazmat/domains/sazmat.com/zzz/storage/logs/cron.log
```

**نکته‌ی مهم:** با `--whisper`، لاگِ خالی به‌تنهایی معنی ندارد — هم می‌تواند یعنی «سالم، کاری نبود» و هم یعنی «کرون مرده». پس این‌طور تستش کن:

چند دقیقه بعد از یک **ساعت گرد** (مثلاً ۱۵:۰۳) لاگ را باز کن. باید دقیقاً دو خط ببینی و بس:

```
  2026-09-20 15:00:03 Running [Callback] .......... 95.58ms DONE
  2026-09-20 15:00:04 Running [Callback] .......... 87.96ms DONE
```

این دو، همان دو کار ساعتی‌اند. اگر بودند، کل زنجیره سالم است.

| چه می‌بینی | یعنی چه |
|---|---|
| دو خط `DONE` سر ساعت | همه‌چیز سالم |
| بعد از ساعت گرد هنوز خالی | کرون اجرا نمی‌شود، یا پنل هاست `\|` را قبول نکرده |
| `Could not open input file: artisan` | مسیر غلط است |
| `Parse error` یا `requires PHP ^8.2` | نسخه‌ی PHP قدیمی است |
| `FAIL` به‌جای `DONE` | خودِ آن کار خطا داده — متن خطا را بخوان |

بعد از یک شبانه‌روز، باید این دو را هم دیده باشی:

```
Running ['artisan' sazmat:cleanup]               ← ۰۳:۰۰
Running ['artisan' sazmat:subscription-reminders] ← ۱۰:۰۰
```

## اگر پنل هاستت `|` را قبول نکرد

بعضی پنل‌های کرون با کاراکتر `|` مشکل دارند. در آن صورت این نسخه را بگذار — لاگ کمی شلوغ‌تر می‌شود ولی کار می‌کند:

```
cd /home/sazmat/domains/sazmat.com/zzz && /opt/alt/php83/usr/bin/php artisan schedule:run --whisper >> /home/sazmat/domains/sazmat.com/zzz/storage/logs/cron.log 2>&1
```

و هر چند ماه یک‌بار محتویات `cron.log` را خالی کن (خود فایل را پاک نکن، فقط محتوایش را).

---


---

# گام ۷ — گوگل سرچ کنسول

## اول: اصلاً این چیه و چرا؟

«سرچ کنسول» یک پنل رایگان از خود گوگل است. دو کار می‌کند:

۱. **به گوگل می‌گویی سایت مال توست.** تا وقتی این را ثابت نکنی، گوگل
   هیچ اطلاعاتی درباره‌ی سایتت به تو نمی‌دهد.

۲. **فهرست صفحه‌هایت را مستقیم تحویل گوگل می‌دهی.** بدون این کار،
   گوگل فقط صفحه‌هایی را پیدا می‌کند که از جایی لینک خورده‌اند. یعنی
   آگهی‌ای که از صفحه‌ی اول جا مانده، عملاً برای گوگل وجود ندارد.

در عوض چیزهایی می‌بینی که هیچ‌جای دیگر نمی‌بینی: مردم با چه کلمه‌هایی
سایتت را پیدا می‌کنند، هر آگهی در نتایج چندم است، و کدام صفحه خطا
دارد.

**این گام اجباری نیست و سایت بدونش هم کار می‌کند.** ولی برای یک سایت
آگهی که همه‌ی درآمدش از گوگل می‌آید، رها کردنش یعنی کور کار کردن.

**چیزی که من از سمت کد انجام دادم:** فایل `sitemap.xml`، فایل
`robots.txt`، و جای آماده برای کد تأیید. کاری که فقط از دست تو برمی‌آید
این است که وارد حساب گوگل خودت شوی — من به حساب گوگل تو دسترسی ندارم.

**وقت لازم: حدود ۱۰ دقیقه، یک‌بار برای همیشه.**

---

## ۷-۱. ثبت سایت و اثبات مالکیت

۱. با مرورگر برو به:

```
https://search.google.com/search-console
```

۲. با همان حساب گوگلی که همیشه استفاده می‌کنی وارد شو.

۳. دو تا کادر نشانت می‌دهد. کادر **سمت راست** با عنوان
   `URL prefix` را انتخاب کن (نه `Domain` را — آن یکی تنظیم DNS
   می‌خواهد و سخت‌تر است). داخلش دقیقاً این را بنویس:

```
https://sazmat.com
```

۴. بزن **Continue**.

۵. حالا گوگل چند روش برای اثبات مالکیت پیشنهاد می‌دهد. روش
   **HTML file** از همه راحت‌تر است چون تو به فایل‌منیجر هاست دسترسی
   داری:

   - روی **Download the file** بزن. یک فایل کوچک دانلود می‌شود با
     اسمی شبیه `google1a2b3c4d5e6f.html`.
   - همان فایل را بدون تغییر نام آپلود کن دقیقاً اینجا:

```
/home/sazmat/domains/sazmat.com/public_html/
```

   - برای اینکه مطمئن شوی درست نشسته، این آدرس را در مرورگر باز کن
     (به‌جای اسم فایل، اسم فایل خودت را بگذار):

```
https://sazmat.com/google1a2b3c4d5e6f.html
```

   اگر یک خط متن دیدی، درست است. اگر صفحه‌ی ۴۰۴ آمد، فایل جای
   اشتباهی رفته.

   - برگرد به گوگل و بزن **Verify**.

> این فایل را هیچ‌وقت پاک نکن. گوگل هر چند وقت یک‌بار دوباره چکش
> می‌کند و اگر نبود، مالکیت را باطل می‌کند.

### اگر روش فایل جواب نداد

روش دوم **HTML tag** است. گوگل چیزی شبیه این نشانت می‌دهد:

```html
<meta name="google-site-verification" content="AbCd1234EfGh5678" />
```

من جایش را از قبل در کد آماده کرده‌ام. فقط این کار را بکن:

۱. فایل `zzz/.env` را باز کن.

۲. این خط را ته فایل اضافه کن — **فقط همان رشته‌ی داخل `content`**،
   نه کل تگ:

```
GOOGLE_SITE_VERIFICATION=AbCd1234EfGh5678
```

۳. فایل را ذخیره کن و کش را پاک کن (همان کاری که در گام ۴ کردی —
   محتویات `zzz/bootstrap/cache/` را خالی کن).

۴. صفحه‌ی اول سایت را باز کن، راست‌کلیک → **View page source**، و
   `Ctrl+F` بزن دنبال `google-site-verification`. اگر تگ آنجا بود،
   برگرد به گوگل و بزن **Verify**.

> تا وقتی این خط را ننویسی هیچ تگی در سایت چاپ نمی‌شود، پس گذاشتن
> این قابلیت روی سایت هیچ ضرری ندارد.

---

## ۷-۲. ثبت نقشه‌ی سایت

این مهم‌ترین قسمت است. نقشه‌ی سایت یعنی فهرست همه‌ی آگهی‌هایت که
خودِ سایت به‌صورت خودکار می‌سازد و هر ساعت به‌روز می‌شود.

۱. اول خودت چکش کن. این آدرس را در مرورگر باز کن:

```
https://sazmat.com/sitemap.xml
```

باید یک صفحه‌ی پر از تگ ببینی. دو چیز را نگاه کن:

- آدرس‌ها با **`https://`** شروع شده باشند، نه `http://`.
- آگهی‌های واقعی‌ات در فهرست باشند.

> اگر آدرس‌ها `http://` بودند: فایل `zzz/.env` را باز کن و مطمئن شو
> خط `APP_URL` دقیقاً این است:
>
> ```
> APP_URL=https://sazmat.com
> ```
>
> بعد کش را پاک کن. (کد جدید خودش این را تشخیص می‌دهد و اصلاح
> می‌کند، ولی درست بودن `APP_URL` به هر حال لازم است.)

۲. در سرچ کنسول، از منوی سمت چپ بزن روی **Sitemaps**.

۳. در کادر `Add a new sitemap` فقط این را بنویس و **Submit** بزن:

```
sitemap.xml
```

۴. وضعیتش باید بشود **Success**. اگر شد `Couldn't fetch`، چند ساعت
   صبر کن و دوباره نگاه کن — گوگل بعضی وقت‌ها با تأخیر می‌خواند.

---

## ۷-۳. تست داده‌ی ساخت‌یافته

من به هر صفحه‌ی آگهی کدی اضافه کرده‌ام که به گوگل می‌گوید این صفحه
یک محصول یا خدمت است، قیمتش چقدر است و چند ستاره گرفته. اگر گوگل
این را قبول کند، آگهی‌هایت در نتایج با ستاره و قیمت دیده می‌شوند —
که یعنی کلیک بیشتر.

۱. برو به:

```
https://search.google.com/test/rich-results
```

۲. آدرس کامل یکی از آگهی‌های واقعی‌ات را بچسبان و **Test URL** بزن.

۳. باید `Product` (برای محصول) یا `Service` (برای خدمت) را تشخیص
   بدهد و زیرش بنویسد `Valid`.

> **درباره‌ی ستاره‌ها:** تا وقتی یک آگهی هیچ امتیازی نگرفته باشد،
> بخش ستاره در این تست دیده نمی‌شود — چون چیزی برای نشان‌دادن نیست.
> این خطا نیست. بعد از اولین امتیاز، ظاهر می‌شود.

---

## ۷-۴. بعدش چی؟

**انتظار واقع‌بینانه داشته باش.** این‌ها یک‌شبه اتفاق نمی‌افتند:

| چه چیزی | چقدر طول می‌کشد |
|---|---|
| گوگل sitemap را می‌خواند | چند ساعت تا ۲ روز |
| آگهی‌ها در گوگل قابل جست‌وجو می‌شوند | ۳ روز تا ۲ هفته |
| بخش `Performance` آمار نشان می‌دهد | حدود ۳ روز بعد از اولین بازدید |
| ستاره در نتایج گوگل ظاهر می‌شود | چند هفته، و فقط اگر آگهی امتیاز گرفته باشد |

بعد از حدود یک هفته، دو جا را نگاه کن:

- **Pages** — می‌گوید چند صفحه ثبت شده و کدام‌ها رد شده‌اند و چرا.
- **Performance** — می‌گوید مردم با چه کلمه‌هایی رسیده‌اند به سایتت.
  این دقیقاً همان چیزی است که به تو می‌گوید عنوان آگهی‌ها را چطور
  بنویسی.

**یک نکته:** هر آگهی جدیدی که ثبت می‌شود، خودکار به نقشه‌ی سایت
اضافه می‌شود. لازم نیست هر بار کاری بکنی. فقط همین یک‌بار ثبتش کن.

---


---

# گام ۸ — تست نهایی، ۹ مورد

چون داده‌ی آزمایشی پاک شده، **اول یک آگهی واقعی ثبت کن**، بعد این‌ها را امتحان کن:

| # | چه کار کن | چه باید ببینی |
|---|---|---|
| ۱ | فرم ثبت آگهی، فهرست استان‌ها | ۳۱ استان، شامل کردستان و سیستان و بلوچستان و لرستان |
| ۲ | یک آگهی محصول باز کن | «افزودن به سبد خرید» نباشد، آدرس دیده شود |
| ۳ | روی «نمایش شماره» بزن | شماره ظاهر شود |
| ۴ | پنل ارائه‌دهنده، کارت «تماس‌های این ماه» | بعد از مورد ۳ عدد بگیرد |
| ۵ | وارد حساب شو، روی ستاره‌ها بزن | «امتیاز شما ثبت شد» بدون رفرش |
| ۶ | زیر ستاره‌ها نظر بنویس و ثبت کن | پیام درشت «نظر شما ثبت شد و به‌زودی روی این صفحه نمایش داده می‌شود» بالای کادر |
| ۷ | پنل ادمین، «نظرهای کاربران» | نظر در صف باشد، تأییدش کن، روی آگهی ظاهر شود |
| ۸ | پنل ارائه‌دهنده، «ویرایش اطلاعات» | آگهی روی سایت تغییر **نکند**، در پنل ادمین «قبل ← بعد» دیده شود |
| ۹ | آدرس پایین را باز کن | ۴۰۴ (توضیح پایین را بخوانید) |

آدرس مورد ۹:

```
https://sazmat.com/payments/sep/order?order=1&Status=1
```

### درباره‌ی مورد ۹

این آدرس، تستِ آن وصله‌ی امنیتی است: قبلاً هر فرد ناشناسی می‌توانست با
حدس‌زدن شماره‌ی سفارش (که عددی ترتیبی است) سفارشِ در انتظار پرداختِ هر
کاربری را لغو کند.

**ولی حالا که سفارش‌های آزمایشی پاک شده‌اند، این آدرس ۴۰۴ می‌دهد** —
چون سفارش شماره‌ی ۱ اصلاً وجود ندارد. کد اول سفارش را پیدا می‌کند و
اگر نبود همان‌جا ۴۰۴ می‌دهد:

```
Order::with('items')->findOrFail($request->integer('order'));
```

پس ۴۰۴ **پاسخ درست** است، ولی چیزی درباره‌ی خودِ وصله ثابت نمی‌کند —
درخواست اصلاً به آن قسمت نمی‌رسد.

**چطور واقعاً تستش کنیم؟** فقط وقتی یک سفارشِ در انتظار پرداخت واقعی
وجود داشته باشد. چون خرید آنلاین فعلاً خاموش است، چنین سفارشی ساخته
نمی‌شود. یعنی این مسیر در عمل الان بلااستفاده است.

**پس این مورد چطور تضمین شده؟** با پنج تست خودکار که سناریوی حمله را
با یک سفارشِ واقعیِ در انتظار پرداخت بازسازی می‌کنند:

```
Anonymous request cannot cancel someone elses order
Wrong token cannot cancel an order
Correct token with failed status does cancel
Anonymous request cannot cancel a subscription
Anonymous callback does not log anyone in
```

این تست‌ها روی کدِ قدیمی اجرا شدند و شکست خوردند، روی کد جدید قبول
می‌شوند. اگر روزی خرید آنلاین را روشن کردی، همین تست‌ها هستند که
نمی‌گذارند این حفره برگردد.

**خلاصه: ۴۰۴ دیدی؟ همه‌چیز درست است، رد شو.**

**برای مورد ۴ یک نکته:** بازدید خودت از آگهی خودت شمرده نمی‌شود. از حالت ناشناس مرورگر تست کن.

---

# گام ۹ — نکته‌های تکمیلی

**آدرس آگهی‌ها از این به بعد فارسی است**، مثل این:

```
/ad/سیمان-تیپ-۲-سنندج
```

آدرس با ویرایش عنوان عوض **نمی‌شود**، تا لینک‌هایی که در گوگل و واتساپ پخش شده‌اند نشکنند.

**خرید آنلاین خاموش است، نه حذف‌شده.** اگر روزی خواستی برگردد، در این فایل مقدار `online_checkout` را `true` کن:

```
zzz/config/marketplace.php
```

**پیام‌های تماس تستی** (۶ تا) هنوز مانده‌اند. پنل ادمین خودش دکمه‌ی حذف دارد. قبل از حذف هر کدام را نگاه کن — شاید بینشان پیام واقعی باشد.

مسیر: پنل ادمین ← «پیام‌های تماس»

**دو چیزی که پیشنهاد دادم ولی هنوز انجام نشده:**

- جست‌وجو فعلاً خوب کار می‌کند ولی از چند هزار آگهی به بعد کند می‌شود و به ایندکس `FULLTEXT` نیاز دارد.
- امتیاز حساب‌های تازه‌ساخته وزن یکسانی با بقیه دارد. اگر امتیازدهی تقلبی دیدی بگو تا وزن‌دهی بر اساس سن حساب اضافه کنم.

---

# اگر چیزی خراب شد

**صفحه‌ی سفید یا خطای ۵۰۰:** اول گام ۴ (پاک‌کردن کش) را دوباره انجام بده.

اگر درست نشد، چند خط آخر این فایل را برایم بفرست:

```
zzz/storage/logs/laravel.log
```

**خطای «Table … doesn't exist»:** یکی از هشت فایل SQL ایمپورت نشده. گام ۲ را دوباره انجام بده.

**تغییرات را نمی‌بینم:** گام ۴ انجام نشده.

---

# ضمیمه — راه جایگزین با کرون (لازم نیست)

اگر یک روز به phpMyAdmin دسترسی نداشتی، یا پاک‌کردن دستیِ کش برایت دردسر بود، یک دستور هست که **کل گام ۲ و گام ۴ را یکجا** انجام می‌دهد.

یک کرون‌جاب **موقت** بساز:

**زمان‌بندی:**

```
*/5 * * * *
```

**دستور:**

```
cd /home/sazmat/domains/sazmat.com/zzz && php artisan sazmat:install --purge-demo >> /home/sazmat/domains/sazmat.com/zzz/storage/logs/install.log 2>&1
```

پنج دقیقه صبر کن، بعد فایل `zzz/storage/logs/install.log` را باز کن. اگر نوشته بود «همه‌ی مراحل با موفقیت انجام شد»، **کرون موقت را حذف کن**.

این دستور دقیقاً همان کار هشت فایل SQL را می‌کند، به‌علاوه‌ی پاک‌کردن کش. اجرای مکررش هم بی‌خطر است.

چند حالت که به‌دردت می‌خورد:

| موقعیت | دستور |
|---|---|
| فقط ببین چه می‌شود، بدون تغییر | `php artisan sazmat:install --dry-run` |
| بدون حذف داده‌ی تستی | `php artisan sazmat:install` |
| فقط پاک‌کردن کش (بعد از ویرایش `.env`) | `php artisan optimize:clear` |

اگر هم SQL را ایمپورت کرده باشی و هم این را اجرا کنی، مشکلی پیش نمی‌آید — هر دو یکدیگر را تشخیص می‌دهند و کار تکراری انجام نمی‌شود.
