@extends('layouts.app') @section('title','تماس با ما') @section('content')<div class="max-w-3xl mx-auto px-4 py-12"><div class="card p-8"><h1 class="text-3xl font-black">تماس با ما</h1><p class="text-sm text-gray-500 mt-2">اگر سوالی دارید یا پیامی برای ما ارسال می‌کنید، حتماً از طریق همان ایمیلی که وارد می‌کنید پاسخ شما را خواهیم داد.</p><form method="POST" action="{{ route('contact.store') }}" class="mt-6 space-y-4">@csrf<input name="name" value="{{ old('name') }}" class="w-full rounded-lg border" placeholder="نام و نام خانوادگی"><input name="email" value="{{ old('email') }}" class="w-full rounded-lg border" placeholder="ایمیل"><input name="phone" value="{{ old('phone') }}" maxlength="15" inputmode="numeric" pattern="[0-9]*" data-digits-only class="w-full rounded-lg border" placeholder="تلفن"><input name="subject" value="{{ old('subject') }}" class="w-full rounded-lg border" placeholder="موضوع"><textarea name="message" rows="6" class="w-full rounded-lg border" placeholder="پیام شما">{{ old('message') }}</textarea><button class="btn-primary">ارسال پیام</button></form></div></div>@endsection
