@extends('admin.layouts.app') @section('content')<h1 class="text-2xl font-black">{{ $city->exists?'ویرایش':'ایجاد' }} شهر</h1><form class="bg-white p-6 rounded-xl mt-5 max-w-lg space-y-4" method="POST" action="{{ $city->exists?route('admin.cities.update',$city):route('admin.cities.store') }}">@csrf @if($city->exists) @method('PUT') @endif<input name="name" value="{{ old('name',$city->name) }}" class="w-full rounded-lg border" placeholder="نام شهر"><select name="province_id" class="w-full rounded-lg border"><option value="">استان</option>@foreach($provinces as $p)<option value="{{ $p->id }}" @selected(old('province_id',$city->province_id)==$p->id)>{{ $p->name }}</option>@endforeach</select><button class="btn-primary">ذخیره</button></form>@endsection
