@extends('admin.layouts.app') @section('content')<h1 class="text-2xl font-black">ویرایش کاربر</h1><form method="POST" action="{{ route('admin.users.update',$user) }}" class="bg-white p-6 rounded-xl mt-5 max-w-xl space-y-4">@csrf @method('PUT')<input name="username" value="{{ $user->username }}" class="w-full rounded-lg border" placeholder="نام کاربری"><input name="name" value="{{ $user->name }}" class="w-full rounded-lg border"><input name="email" value="{{ $user->email }}" class="w-full rounded-lg border"><label><input type="checkbox" name="is_admin" value="1" @checked($user->is_admin)> مدیر</label><button class="btn-primary">ذخیره</button></form>@endsection
