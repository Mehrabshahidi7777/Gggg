@extends('admin.layouts.app') @section('content')<div class="flex justify-between items-center"><h1 class="text-2xl font-black">دسته‌بندی‌ها</h1><a class="btn-primary" href="{{ route('admin.categories.create') }}">جدید</a></div><div class="bg-white rounded-xl mt-5 overflow-auto"><table class="w-full text-sm"><thead><tr><th class="p-3">نام</th><th>نوع</th><th>فعال</th><th>عملیات</th></tr></thead><tbody>@foreach($categories as $c)<tr class="border-t"><td class="p-3">{{ $c->name }}</td><td>{{ $c->type==='product'?'محصول':'خدمت' }}</td><td>{{ $c->is_active?'بله':'خیر' }}</td><td><a class="text-blue-700" href="{{ route('admin.categories.edit',$c) }}">ویرایش</a> <form class="inline" method="POST" action="{{ route('admin.categories.destroy',$c) }}">@csrf @method('DELETE')<button class="text-red-600 mr-2">حذف</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $categories->links() }}</div>@endsection
