@extends('admin.layouts.app') @section('content')<h1 class="text-2xl font-black">{{ $province->exists?'ویرایش':'ایجاد' }} استان</h1><form class="bg-white p-6 rounded-xl mt-5 max-w-lg" method="POST" action="{{ $province->exists?route('admin.provinces.update',$province):route('admin.provinces.store') }}">@csrf @if($province->exists) @method('PUT') @endif<input name="name" value="{{ old('name',$province->name) }}" class="w-full rounded-lg border" placeholder="نام استان"><button class="btn-primary mt-4">ذخیره</button></form>@endsection
