@extends('admin.layouts.app')
@section('content')
<h1 class="text-2xl font-black">خرید و فروش‌ها</h1>
<form class="bg-white p-4 rounded-xl mt-5 flex flex-wrap gap-2"><input name="search" value="{{ request('search') }}" class="rounded-lg border" placeholder="شماره سفارش یا تلفن"><select name="status" class="rounded-lg border"><option value="">همه وضعیت‌ها</option>@foreach(['pending'=>'در انتظار پرداخت','paid'=>'پرداخت‌شده','processing'=>'آماده‌سازی','shipped'=>'ارسال‌شده','completed'=>'تکمیل‌شده','cancelled'=>'لغوشده'] as $k=>$v)<option value="{{ $k }}" @selected(request('status')===$k)>{{ $v }}</option>@endforeach</select><button class="btn-primary">فیلتر</button></form>
<div class="bg-white rounded-xl mt-5 overflow-auto"><table class="w-full text-sm"><thead><tr class="border-b"><th class="p-3 text-right">سفارش</th><th>خریدار</th><th>تلفن</th><th>مبلغ</th><th>فروشنده</th><th>اقلام</th><th>وضعیت</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr class="border-b"><td class="p-3">{{ $order->order_number }}</td><td>{{ $order->buyer->username ?: $order->buyer->name }}</td><td>{{ $order->phone }}</td><td>{{ number_format($order->total_amount) }}</td><td>{{ $order->items->map(fn($i) => $i->seller?->username ?: ($i->seller?->name ?? "حذف‌شده"))->unique()->join("، ") }}</td><td>{{ $order->items_count }}</td><td>{{ $order->status_text }}</td><td><a class="text-blue-700" href="{{ route('admin.orders.show',$order) }}">جزئیات</a></td></tr>@empty<tr><td colspan="8" class="p-6 text-center">سفارشی وجود ندارد.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $orders->links() }}</div>
@endsection
