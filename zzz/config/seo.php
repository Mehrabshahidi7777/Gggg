<?php

return [

    /*
    |--------------------------------------------------------------------------
    | کد تأیید مالکیت گوگل سرچ کنسول
    |--------------------------------------------------------------------------
    |
    | گوگل برای اینکه مطمئن شود سایت واقعاً مال شماست، یک رشته‌ی تصادفی
    | می‌دهد و می‌گوید آن را جایی در سایت بگذار که فقط صاحب سایت
    | بتواند. در روشِ «HTML tag» آن رشته باید داخل تگ زیر در <head>
    | همه‌ی صفحه‌ها بیاید:
    |
    |     <meta name="google-site-verification" content="XXXXXXXX">
    |
    | این مقدار فقط همان XXXXXXXX است - نه کل تگ.
    | یعنی از چیزی که گوگل نشان می‌دهد، فقط محتوای داخل content را
    | بردارید و در فایل .env بنویسید:
    |
    |     GOOGLE_SITE_VERIFICATION=XXXXXXXX
    |
    | اگر خالی بماند، هیچ تگی در صفحه چاپ نمی‌شود. پس گذاشتن این فایل
    | روی سایت، قبل از اینکه کد را داشته باشید، هیچ ضرری ندارد.
    |
    | نکته: بعد از تأیید هم این مقدار را پاک نکنید. گوگل هر از چندی
    | دوباره وجودِ تگ را چک می‌کند و با نبودنش مالکیت را باطل می‌کند.
    |
    */
    'google_site_verification' => env('GOOGLE_SITE_VERIFICATION'),

    /*
    |--------------------------------------------------------------------------
    | اجبارِ https
    |--------------------------------------------------------------------------
    |
    | null  = خودکار (اگر APP_URL با https شروع شود، روشن)
    | true  = همیشه روشن
    | false = همیشه خاموش
    |
    | توضیح کاملِ اینکه چرا این تنظیم برای sitemap و canonical حیاتی
    | است، در app/Providers/AppServiceProvider.php نوشته شده.
    |
    */
    'force_https' => env('FORCE_HTTPS') === null
        ? null
        : filter_var(env('FORCE_HTTPS'), FILTER_VALIDATE_BOOLEAN),

];
