# سازمت — راهنمای کار روی این پروژه

بازار آنلاین مصالح و خدمات ساختمانی. Laravel 12، PHP 8.3 روی هاست،
MySQL 8 در تولید و SQLite در تست‌ها.

## تحویل فایل به کاربر

**همیشه از لینک خام گیت‌هاب.** فایل ضمیمه در چت برای این کاربر اغلب
باز نمی‌شود؛ گیت‌هاب همیشه کار می‌کند. یعنی: زیپ را در ریشه‌ی مخزن
کامیت و پوش کن، بعد لینک بده:

```
https://github.com/Mehrabshahidi7777/Gggg/raw/claude/project-review-improve-1jkgv7/<نام>.zip
```

شاخه‌ی کار: `claude/project-review-improve-1jkgv7`.

زیپ فقط فایل‌های تغییرکرده را داشته باشد، با همان ساختار مسیرِ پروژه،
تا کاربر بتواند مستقیم رونویسی کند.

## محدودیت‌های هاست — مهم

- **`APP_KEY` هرگز نباید عوض شود.** ستون `ads.card_number` با آن
  رمزگذاری شده؛ تغییرش یعنی از دست رفتن شماره‌کارت همه‌ی آگهی‌ها.
- **ترمینال روی هاست در دسترس نیست.** پس `php artisan migrate` اجرا
  نمی‌شود. هر تغییر دیتابیس باید به شکل یک فایل SQL **idempotent** در
  `deploy/` بیاید تا کاربر ایمپورتش کند. فایل باید ردیف مهاجرت را هم
  در جدول `migrations` ثبت کند تا بعداً دوباره اجرا نشود.
- **پرس‌وجوی دلخواه روی دیتابیس ممکن نیست.** کاربر فقط فایل ایمپورت و
  اکسپورت می‌کند؛ کنسول SQL ندارد. پس هرگز نگو «این SELECT را بزن».
  برای دیدن وضعیت، یا از او اکسپورت تازه بخواه، یا کاری کن که خروجی در
  `storage/logs/laravel.log` بنشیند — همان فایلی که می‌تواند بفرستد.
- **`proc_open` روی هاست غیرفعال است.** پس `$schedule->command()` کار
  نمی‌کند و بی‌صدا شکست می‌خورد، در حالی که `cron.log` همان کار را
  `DONE` نشان می‌دهد. هر کار زمان‌بند باید `$schedule->call()` با
  `Artisan::call()` باشد. تستش: `ScheduleRunsWithoutProcOpenTest`.
- **هر کار زمان‌بند باید یک خط در لاگ بنویسد.** خروجی کرون به
  `/dev/null` می‌رود، پس بدون لاگ، «اجرا شد و کاری نبود» و «هرگز اجرا
  نشد» از بیرون یک شکل‌اند — و همین ماه‌ها پنهان ماند.
- **هرگز نگو پوشه‌ای را پاک کند.** فقط رونویسی فایل.
- کرون در دسترس است و کاربر `cron.log` را می‌فرستد.

## ساختار

| چه چیزی | کجا |
|---|---|
| کد لاراول | `zzz/` |
| ویوها | `zzz/resources/views/` |
| CSS منتشرشده | `public_html/css/` (کپیِ روی هاست، نه `zzz/public/`) |
| فونت‌ها | `public_html/fonts/` (خودمیزبان، Vazirmatn) |
| تصاویر | `public_html/images/` |
| SQL استقرار | `deploy/` |

CSS فقط در `public_html/css/` است و نسخه‌ی قرینه‌ای داخل `zzz/` ندارد.

## قواعد طراحی

- **در هر صفحه فقط یک نارنجیِ تو‌پُر.** نارنجی برای کنش اصلیِ همان
  صفحه است؛ بقیه سرمه‌ای پُر، خط‌دار یا نارنجیِ کم‌رنگ (tint).
- در هدر، نارنجی فقط روی «ثبت آگهی» می‌نشیند.
- توکن‌های رنگ در `public_html/css/design-tokens.css`. رنگ‌های متن
  همگی روی کنتراست WCAG AA (۴.۵:۱) تنظیم شده‌اند — قبل از تغییر هر
  رنگ، نسبت کنتراست را حساب کن.
- دیزاین‌سیستم: https://claude.ai/artifact/4BDkzJNhdWFDqvQUJzvJVP

## پیش از تحویل

- کل مجموعه‌ی تست را اجرا کن: `cd zzz && ./vendor/bin/phpunit`
- برای هر باگی که درست می‌کنی تست بنویس، و **با `git stash` ثابت کن
  که تست بدون آن تغییر قرمز می‌شود.** تستی که همیشه سبز است چیزی را
  محافظت نمی‌کند.
- ادعای اندازه و چیدمان را اندازه‌گیری کن، نه حدس. کرومیوم در
  `/opt/pw-browsers/chromium` با Playwright در دسترس است.

## نکته‌های محیط

- پروکسی این محیط `sazmat.com` را می‌بندد؛ سایت زنده دیده نمی‌شود.
  برای بررسی ظاهر باید از کاربر اسکرین‌شات خواست.
- دانلود فونت از CDN هم بسته است.
- پاسخ‌ها به فارسی.
